{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<h1 class="title">Todo List</h1>

{% for task in tasks %}
 <section class="thibzee">
<div class ="container">

<p class="task_title">{{ task.title}}</p>
<hr>
<p>{{ task.content }}</p>
<a href="{{ path('showpage', {id:task.id})}}">Voir la tâche</a>

</div>
</section>
{% endfor %}
{% endblock %}
